/**
 * Multiple embedding sources can be used. Import each one and include it into
 * the exported array.
 */

import embedly from './embed/embedly.jsx';

var all = [
	embedly
];

export default {all};
